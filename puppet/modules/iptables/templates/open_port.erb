<% if !comment.empty? -%>
# <%= comment %>
<% end -%>
<% Array(networks).each { |net| source_spec = (net == '0/0') ? '' : " -s #{net}"
     Array(real_proto).each { |proto| -%>
-A INPUT-FIREWALL<%= source_spec %> -p <%= proto %> -m <%= proto %> --dport <%= number %> -j ACCEPT
<% } } -%>
<% if !comment.empty? -%>

<% end -%>
