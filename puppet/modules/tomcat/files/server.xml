<Server port="8005" shutdown="SHUTDOWN">

  <GlobalNamingResources>
    <!-- Used by Manager webapp -->
    <Resource name="UserDatabase" auth="Container"
              type="org.apache.catalina.UserDatabase"
       description="Read-only user database"
          readonly="true"
           factory="org.apache.catalina.users.MemoryUserDatabaseFactory"
          pathname="conf/tomcat-users.xml" />
  </GlobalNamingResources>

  <Service name="Catalina">

    <!-- HTTP connector for local manager requests only -->
    <Connector address="127.0.0.1" port="8080" enableLookups="false"
               acceptCount="10" />

    <!-- AJP/1.3 connector for requests from Apache -->
    <Connector address="127.0.0.1" port="8009" enableLookups="false"
               protocol="AJP/1.3" tomcatAuthentication="false" />

    <Engine name="Catalina" defaultHost="localhost">
      <Realm className="org.apache.catalina.realm.UserDatabaseRealm"
             resourceName="UserDatabase" />
      <Host name="localhost" appBase="webapps" autoDeploy="false" />
    </Engine>

  </Service>
</Server>
